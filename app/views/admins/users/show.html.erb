<h1>my page</h1>

<%= attachment_image_tag(@user, :image, :fill, 100, 100, fallback: "no_image.jpg", class: "img-thumbnail") %>

<p>ユーザー名: <%= @user.name %></p>

<p>プロフィール: <%= @user.introduction %></p>

<p>所属: <%= @user.affiliation %></p>

<%= link_to "投稿一覧", user_my_post_path(@user), class: "btn btn-primary d-inline-block" %>
<%= link_to "お気に入り投稿", user_favorite_post_path(@user), class: "btn btn-primary d-inline-block" %>
<%= link_to "フォローユーザー", user_following_path(@user), class: "btn btn-primary d-inline-block" %>

<div class="mt-3">
	<%= link_to "削除", user_withdraw_path(@user), method: :delete, class: "btn btn-danger" %>
</div>