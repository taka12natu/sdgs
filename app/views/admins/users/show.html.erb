<h1>my page</h1>

<%= attachment_image_tag(@user, :image, :fill, 100, 100, fallback: "no_image.jpg", size: '100x100') %>

<p>ユーザー名</p>
<%= @user.name %>

<p>プロフィール</p>
<%= @user.introduction %>

<p>所属</p>
<%= @user.category %>

<p>お気に入り投稿</p>
<p>フォローユーザー</p>
<p>投稿一覧</p>
	<table>
		<thead>
			<tr>
				<th>タイトル</th>
				<th></th>
			</tr>
		</thead>
		<tbody>
			<% @posts.each do |post| %>
			<tr>
				<td><%= post.title %></td>
				<td><%= link_to "詳細", adimns_post_path(post) %></td>
			</tr>
			<% end %>
		</tbody>
	</table>