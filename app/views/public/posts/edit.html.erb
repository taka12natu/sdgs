<h1>投稿編集</h1>

<%= form_for @post do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>

	<%= f.label :body %>
	<%= f.text_field :body %>

	<%= f.submit %>

<% end %>

	<%= link_to "投稿を削除する", post_path(@post), method: :delete %>